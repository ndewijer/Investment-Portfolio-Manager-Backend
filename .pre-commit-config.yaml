repos:
  # Universal file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: ['--maxkb=500']
      - id: check-merge-conflict
      - id: mixed-line-ending

  # Go formatting and imports
  - repo: https://github.com/dnephin/pre-commit-golang
    rev: v0.5.1
    hooks:
      - id: go-fmt
        args: ['-w']
      - id: go-imports
        args: ['-w']
      - id: go-build
        args: ['./...']
      - id: go-mod-tidy

  # Fast unit tests with race detector
  - repo: local
    hooks:
      - id: go-test-short
        name: go test (short)
        entry: go test -short -race ./...
        language: system
        pass_filenames: false
        types: [go]

      - id: check-golden-schema-sync
        name: golden_schema.sql must be updated when migrations change
        language: script
        entry: scripts/check-golden-schema.sh
        pass_filenames: false
        files: ^internal/database/(migrations/|testdata/golden_schema\.sql)
